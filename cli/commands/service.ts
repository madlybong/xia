export async function runService(subcommand: string) {
  console.log(`Service manager running: ${subcommand}`);
  console.log('To be implemented: setup systemd / launchd / Windows Task Scheduler depending on OS.');
}
